# GPU 모니터링 v3 계획 리뷰

검토 대상:
- `PLAN.md`
- `feature/auth.md`
- `feature/collect.md`
- `feature/database.md`
- `feature/design.md`
- `feature/manage.md`
- `feature/slack.md`
- `feature/deploy.md`

## 총평

전체 방향은 좋다. `FastAPI + SQLite + SSH pull` 조합은 현재 규모에서 충분히 현실적이고, v1의 단순함과 v2의 UI 장점을 합치려는 목표도 타당하다.

다만 현재 문서는 아래 네 가지가 아직 설계 결정으로 닫히지 않았다.

1. 인증 방식
2. Slack 연동을 위한 네트워크 전제
3. 원격 수집 의존성 (`gpustat`)
4. 실제 배포 방식

이 상태로 구현에 들어가면 중간에 구조를 다시 바꿀 가능성이 높다.

## 주요 이슈

### 1. Slack `/gpu` 설계와 네트워크 전제가 충돌함

문서상 모니터링 서버는 외부에서 직접 접근할 수 없는 구조다.
그런데 Slack slash command 설계는 Slack이 모니터링 서버의 HTTP endpoint로 직접 요청을 보내는 구조를 전제로 한다.

문제:
- 외부 inbound HTTP가 안 되면 `/gpu`는 동작할 수 없음
- 현재 문서만 보면 채널 알림(webhook)은 가능해도 slash command는 불가능할 수 있음

영향:
- Slack 기능이 절반만 구현될 가능성 있음

권고:
- 아래 셋 중 하나를 먼저 확정해야 함
- 모니터링 서버에 외부 ingress 허용
- Slack Socket Mode 사용
- `/gpu` 기능을 MVP에서 제외

### 2. 인증 설계가 문서끼리 서로 충돌함

`PLAN.md`에는 JWT, refresh token, `users` 테이블이 포함되어 있다.
반면 `feature/auth.md`는 계정 시스템이 없고 대시보드는 완전 공개이며, 쓰기 작업만 인라인 패스워드 입력으로 처리한다고 적혀 있다.

문제:
- 구현 범위가 완전히 달라짐
- 백엔드 모델, API, 프론트 상태 관리가 모두 달라짐
- 어느 문서를 기준으로 구현해야 하는지 불명확함

추가 문제:
- 메모 작성 시 사용자의 SSH 비밀번호를 직접 입력받아 검증하는 방식은 보안 경계가 좋지 않음
- key-only 사용자 흐름도 자연스럽지 않음

권고:
- 둘 중 하나로 빨리 정리해야 함
- 진짜 공개 대시보드 + 관리자 작업만 별도 인증
- 최소 계정 시스템 도입 + 쓰기 작업은 세션 기반 인증

현재 방향상으로는 첫 번째가 더 단순하고 계획 의도와도 맞다.

### 3. "SSH 정보만 입력하면 등록 가능"이라는 약속이 실제 수집 방식과 맞지 않음

수집 설계는 원격 서버에 `gpustat --json`이 설치되어 있다고 가정한다.
문서에는 실패 시 `degraded` 상태로 내려간다고 되어 있다.

문제:
- 실제 현장에서는 모든 서버에 `gpustat`가 깔려 있지 않을 수 있음
- 그러면 "에이전트 없이 쉽게 등록"이라는 핵심 가치가 약해짐
- 등록은 성공했는데 핵심 지표(GPU 상태)가 안 나오는 서버가 생길 수 있음

권고:
- `gpustat`를 1순위로 쓰되 `nvidia-smi --query-*` fallback을 반드시 넣는 것이 좋음
- 서버 등록 시 연결 테스트와 함께 GPU 수집 가능 여부도 별도로 검사해야 함
- UI에서 "SSH 연결 성공"과 "GPU 메트릭 수집 가능"을 분리해서 보여주는 편이 맞음

### 4. 배포 계획이 SvelteKit 런타임 모델과 아직 정렬되지 않음

문서는 SvelteKit의 SSR 장점을 채택했다.
그런데 프로덕션 예시는 `npm run preview` 또는 nginx static serve로 적혀 있다.

문제:
- `preview`는 운영 배포 전략으로 보기 어려움
- static serve는 adapter-static 전제 없이는 맞지 않음
- SSR, WebSocket, API 연동 구조까지 고려하면 배포 방식이 더 명확해야 함

권고:
- 아래 중 하나를 먼저 고정해야 함
- `adapter-node`로 프론트 서버를 따로 운영
- 완전 static SPA로 빌드하고 백엔드 API만 별도 운영

현재 계획의 "실시간 + 비교적 단순한 운영"을 고려하면:
- 백엔드 FastAPI
- 프론트 SvelteKit `adapter-node` 또는 정적 빌드 여부를 조기에 결정

이 부분은 초기에 닫는 것이 맞다.

### 5. 서버 정렬 정책이 개인 설정인지 공용 설정인지 불명확함

문서 일부는 쿠키 저장이라고 적혀 있고, 일부는 쿠키 또는 DB 저장이라고 적혀 있으며, 스키마에는 `display_order`가 공용 필드로 들어가 있다.

문제:
- 사용자별 정렬인지 전체 공용 정렬인지에 따라 UI와 API 설계가 달라짐
- 공개 대시보드라면 개인 설정과 공용 설정의 의미가 크게 다름

권고:
- 둘 중 하나를 확정
- 개인별 정렬: 쿠키 또는 local storage
- 전체 공용 정렬: DB 저장 + 관리자만 수정 가능

현재 문맥상 개인 설정으로 두는 편이 단순하다.

### 6. SSH 크리덴셜 중앙 저장에 대한 운영 안전장치가 부족함

문서에는 비밀번호와 private key를 암호화 저장한다고 되어 있다. 이 자체는 맞다.
하지만 운영 측면의 안전장치가 아직 부족하다.

빠진 항목:
- 모니터링 전용 SSH 계정 사용 원칙
- host key 검증 또는 pinning 정책
- 키 교체 절차
- 누가 어떤 서버를 등록했는지에 대한 감시 기준

위험:
- 모니터링 서버 하나가 침해되면 등록된 서버 전체에 연쇄 영향이 갈 수 있음

권고:
- 사용자 개인 계정 대신 읽기 전용 또는 제한된 모니터링 전용 계정 사용
- host key 정책을 문서에 명시
- 등록/수정/삭제에 대한 감사 로그를 남길지 결정

## 확인이 필요한 전제

### 1. Slack inbound HTTP가 가능한가

이게 안 되면 현재 slash command 설계는 바로 수정해야 한다.

### 2. 사용자가 웹 UI에 실제 SSH 비밀번호를 입력하는 흐름을 허용할 수 있는가

이 부분은 기술 문제가 아니라 운영 정책 문제이기도 하다.

### 3. 대상 서버에 `gpustat` 설치 상태를 보장할 수 있는가

보장할 수 없으면 `nvidia-smi` fallback은 사실상 필수다.

## 추천 수정 방향

구현 전에 문서를 아래처럼 정리하는 것이 좋다.

### 우선 결정할 것

1. 인증 모델을 하나로 통일
2. Slack `/gpu`를 유지할지, 네트워크 구조상 다른 방식으로 바꿀지 결정
3. GPU 수집 명령 fallback 전략 확정
4. 프론트 배포 모델 확정
5. 서버 정렬을 개인 설정으로 갈지 공용 설정으로 갈지 확정

### 추천 MVP 재정의

- 공개 읽기 전용 대시보드
- 관리자 인증 기반 서버 등록/수정/삭제
- 메모는 별도 간단 인증 또는 관리자/내부 사용자 전용으로 단순화
- SSH pull 기반 실시간 수집
- `gpustat` 우선, `nvidia-smi` fallback
- Slack 채널 알림 우선 구현
- `/gpu` slash command는 네트워크 가능 여부 확인 후 포함

## 결론

방향은 맞다. 다만 현재 문서는 "좋은 아이디어 목록"에 가깝고, 아직 구현 계약서 수준으로 닫히지는 않았다.

특히 아래 네 가지는 구현 시작 전에 반드시 문서상으로 확정하는 것이 좋다.

1. 인증
2. Slack 네트워크 구조
3. GPU 수집 fallback
4. 배포 방식

이 네 가지가 정리되면 이후 구현은 비교적 안정적으로 진행될 수 있다.
